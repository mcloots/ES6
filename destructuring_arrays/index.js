//let names = ['Michaël', 'Kristof', 'Sarah'];

//create pointer element to destructure our array so we can get the  first name, middle name, last name
let [firstName, middleName, lastName] = ['Michaël', 'Kristof', 'Sarah'];

//change last name
lastName = 'Tinne';

console.log(`${firstName}`, `${middleName}`, `${lastName}`)
