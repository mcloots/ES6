//How to check if array has a value? IndexOf
let numArray = [1,2,3,4,5];

//returns -1 if value can't be found
console.log(numArray.indexOf(0));

console.log(numArray.includes(0)); //returns a boolean value