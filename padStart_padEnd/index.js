//add values to start and end of string
let example = 'Michaël';

//Adds a in beginning of string to meet 10 characters length
console.log(example.padStart(10,'a'));

//Adds a at the end of string to meet 10 characters length
console.log(example.padEnd(10,'a'));